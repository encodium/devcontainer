# Repository Configuration
# Comma-separated list of repositories to clone from encodium organization
REPOS_TO_CLONE=batch,common

# Composer Link Configuration
# Enable/disable automatic linking of common repo to other repositories
COMPOSER_LINK_COMMON=true

# Common Repository Path
# Path where the common repository is located (used by link-common.sh)
COMMON_REPO_PATH=/workspace/common

# MySQL Configuration
MYSQL_HOST=mysql
MYSQL_PORT=3306
MYSQL_USER=dev
MYSQL_PASSWORD=dev
MYSQL_DATABASE=dev
MYSQL_ROOT_PASSWORD=root

# Valkey/Redis Configuration
VALKEY_HOST=valkey
VALKEY_PORT=6379

# LocalStack Configuration
LOCALSTACK_PORT=4566

# Authentication (optional - set these if needed)
# COMPOSER_AUTH=
# NPM_TOKEN=
